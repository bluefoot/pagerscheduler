import { ValidIfDirective } from './valid-if.directive';

describe('ValidIfDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidIfDirective();
    expect(directive).toBeTruthy();
  });
});
